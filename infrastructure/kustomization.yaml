---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Install in order:
  - cert-manager    # TLS certificates
  - longhorn        # Persistent storage
  # - monitoring      # Prometheus + Grafana (disabled - chart timeout, fix later)
  - cloudflared     # Cloudflare Tunnel (public HTTPS ingress)
  - jobrecon-deps   # JobRecon infrastructure (databases, brokers)
